name: CI Pipeline

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirement.txt

      - name: Run tests
        run: pytest

      - name: Build Docker image
        run: docker build -t flask-app .

      - name: Push to Docker Hub
        if: success()
        run: |
          echo "${{ secrets.DOCKER_PASSWORD }}" | docker login -u "${{ secrets.DOCKER_USERNAME }}" --password-stdin
          docker tag flask-app ton-user/flask-app:latest
          docker push ton-user/flask-app:latest
  
  notify:
    runs-on: ubuntu-latest
    needs: test 
    if: failure() 
    steps:
      - name: Envoyer un email en cas d'échec
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: smtp.gmail.com
          server_port: 465
          username: ${{ secrets.MAIL_USERNAME }}
          password: ${{ secrets.MAIL_PASSWORD }}
          subject: "🚨 Tests échoués sur GitHub Actions"
          body: "Les tests unitaires ont échoué sur la branche main. Vérifiez les logs !"
          to: "cyprien.berthelot@edu.ece.fr"
          from: "CI/CD Bot"

